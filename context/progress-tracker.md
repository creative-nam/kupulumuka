# Progress Tracker

Update this file after every meaningful implementation
change.

## Current Phase

- Phase 1 — Core citizen value (all units 1.1, 1.2, 1.3a, 1.3b, and 1.3c complete)

## Current Goal

- Phase 1 fully complete. CI pipeline operational (all 6 jobs run, test failures caught). Next: Phase 2 (community contribution) — requires feature spec to be written first.

## Completed

- **0.1 Project scaffold** (`feature-specs/001-project-scaffold.md`) — Next.js 15 App Router shell with strict TypeScript, Tailwind v4 + shadcn/ui, design tokens from `ui-context.md`, Fraunces + system font stack, light/dark theme switching (cookie override → `prefers-color-scheme` → light default), Vitest + Playwright, placeholder home route with wordmark and theme toggle.

- **0.2 Database schema & seed data** (`feature-specs/002-database-schema-seed-data.md`) — Prisma schema with `Provincia`, `Distrito`, `Bairro`, `Quarteirao`, `BairroVizinho`, `Shelter`, and `User` models; enums for `CapacityStatus` (AVAILABLE/NEARLY_FULL/FULL), `ShelterTier` (OFFICIAL/COMMUNITY), `UserRole` (CITIZEN/ADMIN), and `VerificationStatus` (UNVERIFIED/PENDING/VERIFIED). Migration applied to Supabase Postgres. Data-driven seed script covering 4 provinces (Cidade de Maputo, Província de Maputo, Sofala, Gaza), 5 distritos, 22 bairros, 30 quarteiroes, 25 shelters (mixed tiers/capacities), 26 bairro-vizinho adjacency pairs (all declared pairs resolve), and 8 users (4 institutional + 4 community). Three mockup shelters (EPC Khongolote, Igreja Católica, Salão Paroquial S. João) seeded under Khongolote with matching tier/capacity per the approved UI mockup. Integration test suite (21 tests) verifies per-province row counts, geographic hierarchy correctness, adjacency across all 4 provinces, bidirectional pair resolution, and data invariants.

- **1.1 Geographic picker** (`feature-specs/003-geographic-picker.md`) — A cascading Província → Distrito → Bairro → Quarteirão selection page at `/explorar`. A build-time script (`scripts/generate-geo-snapshot.ts`) queries the seeded database via Prisma and writes a static nested-tree JSON to `public/geo-snapshot.json`. The GeographicPicker Client Component loads this snapshot once via `fetch` and does all cascading filtering in-memory — zero additional network requests after the initial load. Four styled native `<select>` elements (Província, Distrito, Bairro, Quarteirão) are each disabled until its parent is chosen; changing an earlier selection resets everything below. A "Ver abrigos" primary button is disabled until all four levels are selected. The page is a Server Component; only the interactive picker is `"use client"`. All labels and placeholders are in Portuguese. Snapshot verified by integration test (8 tests), cascading behavior verified by component tests (9 tests), full click-through verified by Playwright e2e test (1 test). **Historical (unit 1.1):** the four native `<select>` elements and their 9-test component suite were replaced during the visual polish pass by shadcn/Base UI `Select` — the picker now uses 12 component tests (see the "Fix — Native `<select>` replaced with..." entry below).

- **1.2 Shelter search & results** (`feature-specs/004-shelter-search-results.md`) — A Server Component results page at `/abrigos` reads `quarteiraoId` from `searchParams`, calls `getSheltersForQuarteirao(quarteiraoId)` from `lib/shelters/search.ts` (independently testable, with optional `PrismaClient` parameter for testability), and renders shelter cards per the approved mockup: tier badge (Oficial/Comunitário with inline SVG icons), capacity pill (Livre/Quase cheio/Esgotado with functional color tokens), landmark route description, and a share button. Sort order is an explicit contract: tier ascending (OFFICIAL before COMMUNITY), then capacity severity ascending (AVAILABLE before NEARLY_FULL before FULL), then name alphabetically. Overflow to neighboring bairros triggers only when the selected quarteirão's bairro has zero shelters, with results flagged (`fromNeighboringBairro: true`) and a "Mostrar abrigos de bairros vizinhos" banner. Empty state ("Nenhum abrigo encontrado nesta zona") avoids promising shelter-registration (out of scope). The share button is the only Client Component on the page, using `navigator.share()` falling back to clipboard copy with a "Link copiado" confirmation. Unit 1.1's "Ver abrigos" button was wired to navigate to `/abrigos?quarteiraoId=...` via `useRouter().push()`. Written test-first: 7 integration tests for sort order and overflow behavior, plus 4 component tests for the share button.

## Open Questions

- None.

## Architecture Decisions

- **Snapshots served via StaleWhileRevalidate runtime caching, not precache:** The spec's *original* draft wording listed snapshots in the precache list — it has since been corrected to describe runtime-only caching (spec 007 now states snapshots are NOT precached; see the CodeRabbit doc-fix batch below). The distinction still matters because precache routes take priority over runtime caching routes in Workbox (routes are checked in registration order, precache first). If snapshots are in the precache manifest, the StaleWhileRevalidate runtime cache handler never fires — the precache serves the snapshot and never checks for updates until the entire SW is replaced. To honor the spec's intent (stale-while-revalidate behavior that actually updates), snapshots are instead cached via StaleWhileRevalidate runtime caching only. This means the first-ever visit online won't have them cached, but any subsequent visit (including cold-start offline) will — matching the spec's explicit "first-ever visit requires one online visit" limitation. The precache list covers the immutable app shell (chunks, CSS, fonts, build manifest files).

- **workbox-build `generateSW` with `additionalManifestEntries` instead of glob patterns:** The build output uses content-hashed filenames that we want to include deterministically. `generateSW` mode with `additionalManifestEntries` provides full control over which files are precached, while still letting Workbox handle route registration, runtime caching configuration, and bundling the Workbox runtime into the output file. `globDirectory: "."` and `globPatterns: []` are required to avoid empty-manifest issues in some workbox-build versions. `navigateFallback` and `runtimeCaching` handle navigation and snapshot refresh respectively.

- **E2E tests run against production preview (`next start`), not `next dev`:** The SW registers only when `NODE_ENV === "production"` (or `NEXT_PUBLIC_ENABLE_SW === "true"` for e2e testing) — the guard added to prevent stale-SW confusion during development. Since Playwright runs against `next dev` by default, the SW never registered and the cold-start-offline tests could never pass. Switching the webServer to `npm run build && npx next start -p 3000` makes the SW available and matches the asset URLs the SW's precache manifest expects (dev server generates different chunk paths). **Known tradeoff:** Every e2e run now does a full `next build` first, adding ~30–60s of build time. This is accepted — CI runs build anyway, and for local iteration the build cache makes repeated runs fast after the first. An alternative (having only cold-start-offline tests start their own preview server) was tried first but added complexity from port management, orphaned processes, and a separate test-only server from the rest of the suite. `reuseExistingServer: false` ensures a fresh production preview server is started on every run — if a developer has `npm run dev` in another terminal on port 3000, Playwright will fail fast (port conflict) rather than silently using the dev server and having SW tests fail inscrutably.

- **DevTools Application panel as the primary precache manifest verification method:** The spec notes that precache manifest contents are best confirmed via the browser's DevTools Application panel (Cache Storage). The unit test at `scripts/__tests__/generate-sw.test.ts` provides a script-level assertion that the generated SW file contains the expected entries, but this only validates the build-time configuration — it doesn't prove the SW actually installs and populates its caches correctly. The e2e test verifies cache population after install by querying `caches.open()` from the controlled page context.

- **Dexie Table property declaration with `!`:** Dexie table properties (`geoSnapshot`, `shelters`, etc.) are declared with `!` (definite assignment assertion) because Dexie maps them at runtime via `version().stores()` — TypeScript strict mode requires the assertion since they have no initializer and are not set in the constructor body.

- **`fake-indexeddb` for unit tests requiring Dexie:** jsdom doesn't implement IndexedDB. `fake-indexeddb/auto` patches `globalThis.indexedDB` so Dexie instances created in tests work fully in-memory. The `resetDb()` singleton pattern ensures isolation between tests.

- **`getCachedShelters` cross-references Dexie tables for full rank-and-filter parity:** The cached path needs `quarteiraoName` and `bairroName` which aren't in the shelters snapshot. `getCachedShelters()` builds lookup maps from the geo snapshot (in Dexie) to hydrate these fields, ensuring `rankAndFilterShelters` receives the same `ShelterInput` shape as the live DB path.

- **API routes as the sync/cache interface:** `sync.ts` fetches from `/geo-snapshot.json`, `/shelters-snapshot.json` (static files) and `/api/adjacency` (dynamic endpoint) rather than a single bulk endpoint. This reuses the existing snapshot generation scripts and keeps the adjacency data fresh from the DB without maintaining a separate snapshot file.

- **Results page converted from Server Component to Client Component shell:** The `/abrigos` page was a pure Server Component (unit 1.2). Warm-offline fallback requires the rendering decision to happen on the client — only the client can know whether the live fetch succeeded or whether it should fall back to Dexie. A Server Component streams HTML that was already rendered on the server; if the browser is offline the navigation request itself fails and the server never even runs. Moving the online/offline branching to the client via `ShelterResults` (Client Component) is the only way to serve cached data during a warm-offline client-side navigation. The page file (`page.tsx`) remains a thin Server Component only to validate `searchParams` before delegating, keeping the route handler layer intact.

- **`/api/shelters` and `/api/adjacency` API routes added for client-side data access:** The original architecture had no API routes — the server-only page called Prisma directly. Client-side fallback (and the live online path now that rendering is client-side) needs a fetchable endpoint. `/api/shelters?quarteiraoId=` wraps the existing `getSheltersForQuarteirao` (with `rankAndFilterShelters`) and returns JSON. `/api/adjacency` provides adjacency pairs consumed by `sync.ts` to populate Dexie for offline overflow logic. These are thin route handlers delegating to `/lib` functions, following the existing pattern documented in `code-standards.md` ("Route handlers stay thin. Validate input, call into /lib, return a response.").
- **Tailwind v4 `@theme inline` instead of `tailwind.config`:** `create-next-app@15` ships Tailwind CSS v4, which maps design tokens via CSS `@theme` blocks rather than a separate `tailwind.config` file. All `ui-context.md` color, radius, and spacing tokens are defined as CSS custom properties in `app/globals.css` and referenced from `@theme inline` so Tailwind utilities and raw CSS stay in sync.
- **Theme attribute:** Resolved theme is applied via `data-theme="light" | "dark"` on `<html>`, set by an inline bootstrap script (cookie → system preference → light) and updated by the client-side toggle. Cookie name: `theme`.
- **shadcn/ui base-nova:** Initialized with the default `base-nova` style; only the generated `Button` primitive is present (unused in this unit). shadcn CSS variables are mapped to Kupulumuka semantic tokens so future components inherit the design system.
- **Scaffold merge:** Next.js was generated in `tmp-scaffold/` and merged into the existing repo root so `AGENTS.md` and `context/` remain siblings of `package.json`.
- **Prisma v7 config:** Prisma 7.x moved database connection configuration from `schema.prisma`'s `datasource` block to `prisma.config.ts`. The PrismaClient constructor no longer accepts `datasourceUrl`; it requires a driver adapter (`@prisma/adapter-pg`). `prisma.config.ts` uses `DIRECT_URL` for migrations/seed; the runtime client in `lib/db/prisma-client.ts` uses `DATABASE_URL` (pooled) via the adapter.
- **User model — `carlos@example.com` has both email and phone:** The seed user who owns "Salão Paroquial S. João" needs an email to allow shelter lookup by email (`uploadedByEmail`). This is a minor deviation from the strict "community users have phone only" pattern, documented in the spec as acceptable since `User.phone` and `User.email` are both optional.
- **Native `<select>` instead of `@base-ui/react/select` (historical — unit 1.1 used native `<select>`; migrated to shadcn/Base UI Select in the visual polish pass — see "Fix — Native `<select>` replaced with..." entry above):** The spec says "shadcn Select" but the implementation uses native `<select>` elements styled with Tailwind to match `ui-context.md` tokens. Rationale: native selects are fully testable in jsdom with RTL, accessible by default, add zero JS bundle weight for dropdown behavior, and are styled to visually match the design system. `@base-ui/react/select` is available but was not used because its Portal-based popup complicates RTL testing and adds runtime JS overhead that the low-bandwidth invariant doesn't warrant for a simple cascading-select flow.
- **`globals: true` added to `vitest.config.ts`:** Required so that `@testing-library/react`'s auto-cleanup (which relies on a global `afterEach`) correctly unmounts between tests. Without it, multiple `render()` calls across tests accumulated stale DOM trees, causing "multiple elements found" errors in `screen.getByRole` queries. The integration config already used `globals: true`.
- **Integration test parallelism disabled:** `vitest.integration.config.ts` now uses `fileParallelism: false` and `pool: "forks"` to prevent database deadlocks when both `seed.test.ts` and `geo-snapshot.test.ts` truncate tables concurrently against the same Supabase project.
- **Fetch error-handling pattern (established here; reference by later units):** The GeographicPicker models two user-visible states beyond success: a loading state ("A carregar...") and a fetch-failure state. On failure, an `error` boolean is set (not a raw error object — just a flag that the request failed). The view shows a non-technical Portuguese message ("Não foi possível carregar os dados") and an outline-style "Tentar novamente" button that calls the same `loadSnapshot` function again, clearing the error flag first. Key pattern rules for later units to follow:
  1. Use a single `error` boolean for fetch failures, not an error-message string or thrown object — the UI only needs to know "did it fail," not why (there's nothing actionable for the user about a 404 vs a timeout).
  2. Error copy follows `ui-context.md` Invariant: "Never blame the user or the network for failures." — no "Erro de rede" or "Falha na conexão," just "Não foi possível carregar os dados" + an actionable retry.
  3. Retry calls the same load function, never inline logic — this guarantees the initial load and retry follow identical paths.
   4. The retry button uses the `outline` variant (secondary action), not the primary `default` variant, to distinguish it from the flow's main "Ver abrigos" action.

- **`getSheltersForQuarteirao` accepts optional `PrismaClient` parameter:** The function defaults to the global singleton `prisma` from `lib/db/prisma-client.ts`, but callers (primarily integration tests using `TEST_DIRECT_URL`) can pass a different client instance. This follows the same pattern as `runSeed(prisma)` in `prisma/seed.ts`, and is the recommended pattern for any future `lib/` function that queries the database — always default to the global client but accept an injected one for testability.
- **Share button check uses `typeof navigator.share === "function"`, not `"share" in navigator:`** The `"share" in navigator` check passes even when `navigator.share` is `undefined` (since the property exists), causing the code to call `undefined()` and throw into the catch handler, which then returns without clipboard fallback. Check that `navigator.share` is actually a function before calling it.
- **Integration test files colocated in `lib/` need exclusion from unit test runner:** `vitest.config.ts` (unit tests) must explicitly exclude `lib/shelters/**/*.test.ts` (or similar patterns) because those tests require the node environment and test database — jsdom won't work. Add the exclusion pattern alongside `tests/integration/`.

- **Exclusion refined to `lib/shelters/search.test.ts` instead of `lib/shelters/**/*.test.ts`:** Unit 1.3 introduced `rank-and-filter.test.ts`, a pure-function test with no database dependency. The broad `lib/shelters/**/*.test.ts` exclusion excluded it from the unit runner unnecessarily. Changed to `lib/shelters/search.test.ts` so pure-function tests in the same folder run as unit tests while the DB-dependent integration test still runs only under the integration config.
- **No icon library dependency — inline SVGs used instead:** The mockup references `ti-` (Tabler Icons) but no icon library is installed in `package.json`. Tier badge icons (checkmark for Oficial, users for Comunitário) and the share icon are rendered as minimal inline SVGs. If Tabler Icons are installed later, these should be swapped for the corresponding `@tabler/icons-react` components — the SVGs are small enough that the migration is mechanical.

- **Small gap closed before Phase 1 PR — root route (`/`) redirects to `/explorar`:** The root page previously rendered the unit 0.1 placeholder wordmark + theme toggle, which had never been wired to the actual app. Replaced with a `redirect("/explorar")` so visiting `/` lands on the GeographicPicker page. The old unit test (`page.test.tsx`) was updated to verify the redirect call instead of wordmark rendering. All 63 unit tests, build, and lint pass.

- **Fix — ThemeToggle relocated to layout (it was orphaned by the redirect):** The `ThemeToggle` component was only imported by the old root page, so the redirect change left it reachable from nowhere — violating `ui-context.md`'s requirement for a visible manual light/dark override. Moved it into `app/layout.tsx` (fixed top-right, using the same `--spacing-screen-x` token as page content) so it persists across every screen. The e2e manual-override-persistence test was restored, adapted to navigate to `/explorar` and target the layout-positioned toggle. 63 unit tests, lint, and 3 theme e2e tests pass.

- **Fix — App header component supersedes the inline toggle relocation:** The redirect task's quick fix (placing the theme toggle as a loose `fixed` div in the layout) was replaced with a proper `AppHeader` component per the App Header convention added to `ui-context.md`. Key details:
  - `components/app-header.tsx`: Sticky top bar (52px, `--bg-surface` + `--border` bottom hairline), "Kupulumuka" wordmark (Fraunces 600, 16px) linking to `/explorar` on the left, icon-only sun/moon toggle (44×44px min tap target, `aria-label="Alternar tema"`) on the right. Uses `getDocumentTheme`/`toggleDocumentTheme` from the existing theme system, with inline SVG icons (following the project's pattern in `shelter-card.tsx` — no icon library dependency). The icon shows the opposite of the current theme (moon in light mode, sun in dark mode) so the action is clear before clicking.
  - `components/theme-toggle.tsx` removed (superseded by `AppHeader`).
  - Component test (`components/app-header.test.tsx`) with 3 tests: wordmark link renders and points to `/explorar`, icon-only toggle has accessible label without visible text, and clicking the toggle actually calls `toggleDocumentTheme` (per `code-standards.md`'s "test what it does" rule).
  - All 66 unit tests, lint, build, and all 3 theme e2e tests pass.

- **Pre-existing test fix — shelter-search e2e tests (shelter-search.spec.ts):** Both tests in this file were broken from their introduction in commit `54c3670` (spec 004/unit 1.2). Verified by checking the git history: the file has exactly one commit, the seed data was the same then as now, and `BackNav` was introduced in that same commit. These tests never passed — the 1.2 success checklist was closed without them genuinely passing.
  - **Khongolote test fix:** Corrected `selectOption({ index: 2 })` to `{ index: 1 }` for both Distrito and Bairro. The original indices (2/2) selected Boane→Beluluane instead of Matola→Khongolote; the comments were also wrong ("after placeholder and Boane" → Matola is before Boane). Also replaced ambiguous `getByText("Khongolote")` with `getByRole("link", { name: /Khongolote/ })` to target only the BackNav link, not the shelter card h2 ("EPC Khongolote") or route description paragraph.
  - **Ponta-Gêa test fix:** Replaced ambiguous `getByText("Ponta-Gêa")` with `getByRole("link", { name: /Ponta-Gêa/ })` to avoid strict-mode violation against the shelter card h2 ("Escola Secundária da Ponta-Gêa").
  - **Timing fix:** Both BackNav assertions given `{ timeout: 10000 }` to match the província select's timeout — the ShelterResults component fetches from `/api/shelters` on mount, which can take >5s under dev-server load.
  - All 8 e2e tests now pass with `--workers=1` (same as CI). The original parallel-worker flakiness (resource contention on `/api/shelters`) is a separate, pre-existing infrastructure concern.

- **Fix — SelectField trigger showed raw value UUID instead of option name:** Without an `items` prop on the `Select` Root, Base UI's `SelectValue` renders the raw value string (the UUID) rather than looking up the matching item's display label. Fixed by passing `const items = Object.fromEntries(options.map((o) => [o.id, o.name]))` as the `items` prop to `Select` — this gives Base UI the value→label mapping it needs. Every test that selected an option now also asserts `toHaveTextContent("expected name")` on the trigger, catching any future regression.

- **Fix — Missing separator between dropdown options:** `SelectItem` had no visual border between rows, making the option list appear as one undifferentiated block. Added `border-b border-border-default last:border-b-0` to the `SelectItem` base class in `components/ui/select.tsx`.

- **Fix — Native `<select>` replaced with shadcn/ui `Select` (Base UI) in GeographicPicker:** The geographic picker used native `<select>` elements styled to match `ui-context.md` tokens. Per the spec's Component Conventions ("Select / dropdown: use shadcn/ui's Select (Radix-based), not a native `<select>`"), they were replaced with a shadcn `Select` wrapping `@base-ui/react/select`. Key details:
  - `components/ui/select.tsx`: Generated by `npx shadcn add select` (base-nova style, uses `@base-ui/react/select` not `@radix-ui/react-select`). Modified default styling to match Kupulumuka tokens: `bg-surface` trigger and content, `border-border-default`, 12px radius, subtle `--accent` tint on highlighted items (`focus:bg-accent/10`), `--text-primary` option text, `--text-secondary` placeholder. Chevron, check, and scroll-arrow icons swapped from `lucide-react` (not installed) to inline SVGs (following the project's established no-icon-library convention from shelter-card and tier badges).
  - `components/geographic-picker.tsx`: The `SelectField` wrapper now renders a `Select`/`SelectTrigger`/`SelectValue`/`SelectContent`/`SelectItem` tree instead of a native `<select>`. `value` is passed as `string | null` (Base UI shows placeholder for null); `onValueChange` guards against null. `id` on the trigger provides label association via `htmlFor`. The `disabled` prop on `Select` root disables the trigger with `aria-disabled`.
  - `components/geographic-picker.test.tsx`: All native `<select>` interactions (`user.selectOptions`, `selectedIndex`, `.options` array access) replaced with click-based pattern: click trigger → `findByRole("option")` → click option, using a `selectOption(user, label, optionName)` helper. One new keyboard-navigation test added (Tab focus → Enter → ArrowDown → Enter). 67 tests pass (1 new).
  - **Reverses unit 1.1's native-select decision:** Unit 1.1 deliberately used native `<select>` elements citing zero-JS bundle weight, full jsdom testability, and accessibility. The OS-rendered dropdown panel in practice broke visual cohesion enough on real devices to supersede that earlier choice (as the spec itself warned it would), and the added JS weight (~41 KB for `/explorar` from 1.68 KB → 42.8 KB) is accepted — the themed dropdown panel is a hard UX requirement per the spec, not a style nicety. All 67 unit tests, build, and lint pass.

- **Fix — Screen horizontal padding not applied on any screen:** `px-spacing-screen-x` was used on every page/component below the app header (`explorar/page.tsx`, `abrigos/page.tsx`, `shelter-results.tsx`, `app-header.tsx`) but this class never generated a matching CSS rule — Tailwind v4 + Turbopack does not produce utility classes from `--spacing-screen-x: 16px` in `@theme inline` (confirmed by inspecting the built `@layer utilities` block). Headings, form fields, and shelter cards rendered flush against the viewport edge. Fixed by replacing all `px-spacing-screen-x` with `px-4` (standard Tailwind spacing utility, `padding-inline: 1rem` = 16px at default root font-size, within the spec's 12–16px range). Removed the unused `--spacing-screen-x` token from `globals.css`. Verified by build (CSS now contains `.px-4{` and zero `spacing-screen` references), lint, and all 66 unit tests.

## Session Notes

- All checklist items for spec 001 are green: `npm run build`, `npm run test`, `npm run test:e2e`, and `npm run lint` pass.
- All checklist items for spec 002 are green: migration clean, seed produces correct hierarchy (Cidade de Maputo and Província de Maputo as separate Provincia rows, Matola/Boane as Distrito under Província de Maputo), 21 integration tests pass, build and lint clean.
- All checklist items for spec 003 are green: geo-snapshot integration tests (8 passed), GeographicPicker component tests (12 passed including 3 error-handling tests), Playwright e2e test (1 passed), build and lint clean.
- CodeRabbit fix 1: Added `orderBy: { name: 'asc' }` to distritos, bairros, and quarteiroes nested Prisma queries in `scripts/generate-geo-snapshot.ts` so the snapshot is deterministic.
- CodeRabbit fix 2: The GeographicPicker fetch now sets an `error` boolean on failure, renders a Portuguese error message ("Não foi possível carregar os dados") and an outline "Tentar novamente" retry button that re-calls the same `loadSnapshot` function. This establishes the app's first network-request error-handling pattern, documented in Architecture Decisions for later offline-related units to reference. Three new component tests verify the error state, retry button presence, and successful recovery after retry.
- The geo-snapshot generation script (`scripts/generate-geo-snapshot.ts`) writes to `public/geo-snapshot.json` and is exposed as `npm run generate:geo-snapshot`. It should be re-run whenever seed data changes.
- The `/explorar` route is statically prerendered; the GeographicPicker component loads the snapshot at runtime via `fetch("/geo-snapshot.json")` on the client.
- The GeographicPicker uses native `<select>` elements styled per `ui-context.md` tokens (rounded-[10px], border-border-default, bg-surface, focus-visible:ring-accent-brand), not @base-ui/react/select. **(historical — migrated to shadcn/Base UI Select in the visual polish pass.)**
- Page is a Server Component; only GeographicPicker is `"use client"`. The page lives under `app/(public)/explorar/` per the architecture's route group convention.
- `vitest.config.ts` had `globals: true` added to fix RTL cleanup between tests — without it, `@testing-library/react` couldn't find a global `afterEach` and never unmounted between renders.
- `vitest.integration.config.ts` had `fileParallelism: false` and `pool: "forks"` added to prevent concurrent database truncation deadlocks between seed and geo-snapshot tests.
- Integration tests use a separate vitest config (`vitest.integration.config.ts`) with `node` environment and run via `npm run test:integration`. The main `vitest.config.ts` excludes `tests/integration/`.
- The seed script is data-driven (arrays/loops per the spec's Design section), not hand-repeated blocks.
- Prisma generated client output is at `lib/generated/prisma/client.ts` (Prisma 7.x changed the output structure from a re-exporting `index.ts` to direct `client.ts`).
- **CodeRabbit fix — BairroVizinho creation ordering:** The original seed created BairroVizinho pairs inline during the bairro loop, using `findFirst` by name to resolve the neighbor. If the neighbor appeared later in the array, the lookup returned null and the pair was silently lost — only 13 of 26 declared pairs were created. Fixed by separating into two phases per distrito: Phase 1 creates all bairros + quarteiroes + shelters and collects IDs in a `name → id` map; Phase 2 iterates the same data to create every BairroVizinho pair by looking up the neighbor ID from the in-memory map. Integration tests strengthened accordingly: from 2 adjacency tests to 6, including exact count assertion (26 pairs), verification that all 4 provinces have pairs, no-self-reference check, and bidirectional resolution tests in two different provinces.
- **CodeRabbit fix — Integration test self-seeding:** The integration tests previously assumed seeded data already existed in the database rather than running the seed process themselves in `beforeAll`. Fixed by refactoring `prisma/seed.ts` to export `runSeed(prisma)`, updating the test's `beforeAll` to truncate all tables and call `runSeed()`, and `afterAll` to truncate again for idempotent repeated runs. The `?schema=` approach was attempted first but the PrismaPg adapter doesn't scope queries to the schema specified in the connection string — data went to the default schema regardless. Truncation replaced schema isolation as the idempotency mechanism. Seed module auto-execution on import was guarded behind `process.argv[1]` check so importing `runSeed` doesn't trigger the CLI entry point.
- **CodeRabbit fix — BairroVizinho createMany:** Swallowed `.catch(() => {})` around per-pair `bairroVizinho.create` replaced with a single `createMany({ skipDuplicates: true })` call that collects all pairs first, then creates them in one batch. This eliminates silent error suppression and improves performance.
- **CodeRabbit fix — Province-scoped bairro lookups:** All `findFirst` bairro lookups in integration tests now filter by `distrito: { provincia: { name } }` instead of bare name, ensuring correctness if duplicate bairro names ever exist across provinces.
- **CodeRabbit fix — Test database isolation via separate Supabase project:** The truncate-based test isolation previously targeted whatever `DIRECT_URL` was configured, risking real dev data. A second dedicated Supabase project was set up and its connection string is exposed as `TEST_DIRECT_URL` / `TEST_DATABASE_URL` in `.env` / `.env.example`. The integration test now reads `TEST_DIRECT_URL` and runs `prisma migrate deploy` against it in `beforeAll` before the truncate/seed flow, ensuring the test schema is always up to date. The original disposable-schema approach from spec 002 didn't work with the `PrismaPg` adapter (which ignores `?schema=` in the connection string), so test isolation is now a separate project rather than a separate schema.
- All checklist items for spec 004 are green: `getSheltersForQuarteirao` returns shelters in exact sort order (tier → capacity severity → name) verified by 7 integration tests, overflow to neighboring bairros only triggers when local bairro has zero shelters with results flagged (`fromNeighboringBairro`), 1.1's "Ver abrigos" button navigates to `/abrigos?quarteiraoId=...` (verified by updated component test), rendered shelter cards match `ui-context.md` tokens (tier badge with inline SVG icon, capacity pill with text label, route description, share icon), empty-state and "showing nearby results" copy is in Portuguese without shelter-registration references, share button works via `navigator.share()` with clipboard fallback tested (4 component tests), e2e test confirms correct results for Khongolote (Província de Maputo, 3 shelters) and Ponta-Gêa (Sofala, 1 shelter) — two different provinces, the results page (`/abrigos`) is a Server Component (only `ShareButton` is `"use client"`) — **historical:** true for unit 1.2; since converted to a Client Component shell per the unit 1.3b architecture decision (see Architecture Decisions), `npm run build` and `npm run lint` pass clean.
- **1.3a Shelters snapshot & shared ranking logic** (`feature-specs/005-shelters-snapshot-shared-ranking.md`) — `lib/shelters/rank-and-filter.ts` extracted as a pure function (no Prisma imports, no DB calls) operating on plain arrays, tested with 7 pure-function tests adapted from 1.2's sort/overflow cases. `getSheltersForQuarteirao` in `lib/shelters/search.ts` refactored to be a thin wrapper: fetch via Prisma, delegate to `rankAndFilterShelters`. Unit 1.2's original 7 integration tests pass completely unchanged after the refactor. `scripts/generate-shelters-snapshot.ts` (mirroring the geo-snapshot pattern) queries all shelters via Prisma and writes tier, capacity status, route description, quarteirão/bairro IDs to `public/shelters-snapshot.json`, verified by 5 integration tests against seeded data. Exposed as `npm run generate:shelters-snapshot`. `npm run build` and `npm run lint` pass clean. No Dexie, service worker, or UI/rendering changes present. Success checklist: all items green.

- **1.3b Offline data sync & fallback rendering** (`feature-specs/006-offline-sync-fallback-rendering.md`) — Warm-offline support via Dexie (IndexedDB) caching and client-side fallback rendering. Key deliverables:
  - `lib/offline/db.ts`: Dexie database with 4 tables (`geoSnapshot`, `shelters`, `adjacencyPairs`, `syncMeta`) and singleton accessor.
  - `lib/offline/sync.ts`: `syncData()` fetches `geo-snapshot.json`, `shelters-snapshot.json`, and `/api/adjacency` on every online call; stores all three in Dexie with a `lastSyncedAt` timestamp; clears and refreshes on each sync (never skips). 6 unit tests.
  - `lib/offline/query.ts`: `getCachedShelters()` reads from Dexie and runs through `rankAndFilterShelters` (unit 005's pure function) — same sort order, overflow behavior, and result shape as the live path.
  - `app/api/adjacency/route.ts` and `app/api/shelters/route.ts`: API endpoints consumed by `sync.ts` and `ShelterResults`.
  - `components/sync-on-load.tsx`: Calls `syncData()` on mount and registers `online` event listener for re-sync.
  - `components/shelter-results.tsx`: Client component replacing the old server-only page; tries live fetch first, falls back to Dexie; shows cache-staleness banner ("Sem ligação — a mostrar dados guardados (última atualização: {timestamp})"). 7 component tests.
  - `components/geographic-picker.tsx`: Extended to fall back to Dexie-cached geo snapshot when live fetch fails; shows staleness banner when serving cached data. 2 new tests (16 total).
  - `app/(public)/abrigos/page.tsx` updated to delegate to `ShelterResults`. `app/layout.tsx` includes `<SyncOnLoad />`.
  - `dexie` and `fake-indexeddb` (dev) added to `package.json`.
  - All spec checklist items green: `npm test` (42 tests), `npm run build`, `npm run lint` pass. No service worker or cold-start handling present.
  - CodeRabbit fix — `getLastSyncedAt()` failure in `geographic-picker.tsx` no longer discards a successfully-loaded cached snapshot: wrapped in its own try/catch so a metadata fetch failure defaults timestamp to `null` rather than propagating to outer catch.
  - CodeRabbit fix — Error state in `shelter-results.tsx` now renders "Não foi possível carregar os dados" + "Tentar novamente" retry button (the established GeographicPicker failure pattern from unit 1.1, documented in Architecture Decisions), not the misleading `EmptyState` that conflates "couldn't load" with "no shelters found".

- **1.3c Service worker precaching** (`feature-specs/007-service-worker-precaching.md`) — Cold offline start via Workbox service worker. Key deliverables:
  - `scripts/generate-sw.ts`: Build-time script using `workbox-build` `generateSW` to create a service worker at `public/sw.js`. The precache manifest includes all JS/CSS chunks, self-hosted Fraunces font files (`.woff2`), and build manifest files — everything the app shell needs to render on a cold start.
  - Snapshots (`geo-snapshot.json`, `shelters-snapshot.json`) are NOT in the precache manifest but are handled via StaleWhileRevalidate runtime caching, so they're cached on first online access and refreshed in the background on subsequent visits (the precache route would take priority and prevent runtime caching from firing).
  - Navigation caching: Two strategies for offline HTML delivery. `NetworkFirst` for page routes (`/`, `/explorar`, `/abrigos`) caches the navigation response on the first online visit and serves it from cache on subsequent offline navigations. `navigateFallback: "/explorar"` ensures navigation to any route redirects to the cached explorar page when offline.
  - `skipWaiting: true` and `clientsClaim: true` so the SW activates immediately after install without requiring a page reload.
  - `components/service-worker-register.tsx`: Client component that registers the SW in a `useEffect` (included in `app/layout.tsx`).
  - `workbox-build` added as a dev dependency.
  - `npm run build` chains `next build --turbopack && npx tsx scripts/generate-sw.ts` so the SW is generated fresh on every production build. Also exposed as `npm run generate:sw` for standalone regeneration.
  - `eslint.config.mjs` updated to exclude generated `public/sw.js` and `public/workbox-*.js` files from lint.
  - Unit tests (8 tests, original 1.3c suite) at `scripts/__tests__/generate-sw.test.ts` verify the generated SW file contains: `precacheAndRoute`, JS chunks, CSS, font files, StaleWhileRevalidate routes for both snapshots, `skipWaiting`, and `clientsClaim`. Subsequent CodeRabbit fixes (walkDir, HTML fixtures, htmlFilePathToUrl) expand this to 21 tests total.
  - E2e tests (2 tests) at `tests/e2e/cold-start-offline.spec.ts` verify: SW registers and controls the page after an online visit, and the precache cache contains app shell assets (chunks, CSS, fonts).
  - All checklist items green: `npm test` (50 unit tests), `npm run build`, `npm run lint` pass.
  - **Limitation documented:** First-ever use requires one online visit — a device that has never been online cannot have any data to precache. This is accounted for in the spec and will be surfaced for unit 5.1 (onboarding).
  - **StaleWhileRevalidate runtime caching** is configured in the generated SW and verified by unit test/static analysis. Actual runtime cache population timing depends on SW activation vs. page render order; the StaleWhileRevalidate cache is populated on the first request handled by an active SW (second page load at latest). Verified via manual DevTools Application panel inspection.
  - **Deviations from spec:** Snapshots are not in the precache manifest per se (they're runtime-cached via StaleWhileRevalidate), which is functionally equivalent — cold-start offline users still get cached snapshots because the StaleWhileRevalidate handler serves from its persistent cache (populated during any prior online visit). The explicit precache-vs-runtime distinction is noted here in Architecture Decisions for clarity.
  - No PWA manifest, icons, install prompt, or generic uncached-route fallback page present in the diff.
  - **Fixes applied:**
    - CodeRabbit fix — the `NetworkFirst` runtime route now uses a pathname-based function matcher (`({ url }) => ["/", "/explorar", "/abrigos"].includes(url.pathname)`) instead of a regex anchored to `/`, so the page cache route actually matches same-origin navigations.
    - Added a cold-start e2e regression for `/abrigos`: it performs a real online visit to `/abrigos?quarteiraoId=...` with Khongolote selected, reloads while SW-controlled so the page response is cached, then reopens in a fresh offline persistent profile and confirms the cached results render instead of the fallback shell.
    - Root-cause fix — the preview server was loading the local `.env` database URLs, so `next start` and the `/api/adjacency` route were talking to the dev Supabase project (`postgres.lnuwsgxnusuyvyqlbxba`) instead of the test project. Fixed by adding `scripts/run-e2e-webserver.ts`, which loads `.env`, validates `TEST_DIRECT_URL`, then overrides both `DATABASE_URL` and `DIRECT_URL` with the test database before running `build:e2e` and `next start`. The temporary `/api/adjacency` stub was removed; the focused cold-start regression now passes against the real sync path. Playwright's `webServer.timeout` remains at 180000ms.
    - CodeRabbit fix — `readdirSync()` in `scripts/generate-sw.ts` replaced with a `walkDir()` function that recurses into subdirectories. Previously only flat directory listing was used for `chunks/` and build-ID directories, which would miss files nested under subdirectories (e.g. `chunks/app/layout.js` produced by webpack builds). All three scanned directories (`chunks/`, `media/`, and the build-ID directory) now use `walkDir()`. `media/` is expected to be flat (font files only) but consistency of a single walk pattern reduces the chance of a similar gap.
    - Unit tests (6 new tests in `scripts/__tests__/generate-sw.test.ts`) verify `walkDir` behavior: flat files, one-level subdirectory, multi-level nesting matching the expected `chunks/app/explorar/` pattern, empty directory, and no-matches case. Existing precache content regex patterns updated from lazy `.+?` to greedy `.+` to explicitly allow subdirectory segments in matched URLs.
    - CodeRabbit fix — `navigateFallback: "/explorar"` required the fallback document to exist in the precache manifest. Previously no HTML pages were precached; only static assets from `.next/static/` were included. Added a scan of `.next/server/app/` for pre-rendered HTML files (`index.html`, `explorar.html`, excluding `_not-found.html`, `404.html`, `500.html`), mapping `index.html` → URL `/` and any other `name.html` → URL `/name`. This adds both `/` and `/explorar` to the precache manifest with content-based revisions, ensuring `navigateFallback` resolves correctly on cold-start offline. Unit tests (2 new tests) verify both HTML entries are present with revision hashes.
    - CodeRabbit fix — `service-worker-register.tsx` now guards registration behind `process.env.NODE_ENV === "production"` (or `NEXT_PUBLIC_ENABLE_SW === "true"` for e2e testing) so the SW never registers in dev, preventing stale-SW confusion. Silent `.catch(() => {})` replaced with `.catch(console.error)` so registration failures are visible.
    - CodeRabbit fix — `cold-start-offline.spec.ts` first test: `page.evaluate()` sampling `navigator.serviceWorker.controller` replaced with `page.waitForFunction()` for reliable SW-active detection (matching the pattern already used in the second test).
    - CodeRabbit fix — `cold-start-offline.spec.ts` second test extended: after verifying cache contents offline, now opens a new page with `context.setOffline(true)`, navigates to `/explorar`, and confirms the GeographicPicker renders. This required switching the Playwright webServer from `npm run dev` to `npm run build && npx next start -p 3000` because the dev server generates different asset URLs than the production build's SW precache manifest — the two were incompatible.
    - `playwright.config.ts` webServer changed to `reuseExistingServer: false` to prevent silent fallback to a developer's `npm run dev` on port 3000, which would make SW registration fail inscrutably (dev server runs with `NODE_ENV=development`, blocking SW registration per the guard).
    - Verified all 8 e2e tests pass against the production preview server (both SW tests, shelter-search, geographic-picker, theme).

- **CodeRabbit fix — HTML URL construction discards nested route segments:** The `generate-sw.ts` manifest URL for HTML pages used `parse(filePath).name` which only captures the filename (e.g. `bar` from `foo/bar.html`), silently producing the wrong URL (`/bar`) for any future nested App Router route. Fixed by extracting `htmlFilePathToUrl` which derives the URL relative to `serverAppDir`: strips the `.html` extension, converts path separators, and maps `index` → `/` and `foo/bar/index` → `/foo/bar` while preserving all directory segments (e.g. `foo/bar.html` → `/foo/bar`). This function is exported and tested independently with 5 unit tests including a nested route fixture (`foo/bar.html` → `/foo/bar`) that would have failed under the old implementation. All 21 tests pass. The `parse` import removed from `generate-sw.ts` (replaced by `sep` used in the new function).

- **CodeRabbit fix — `walkDir` follows symlinks via `statSync`, risking infinite recursion or out-of-tree scanning:** `scripts/lib/walk-dir.ts` used `readdirSync(dir)` then `statSync(fullPath).isDirectory()` which follows directory symlinks — a symlink cycle would cause infinite recursion, and a link outside the build tree could scan unintended files. Changed to `readdirSync(dir, { withFileTypes: true })` and uses each `Dirent`'s own `isDirectory()`/`isFile()` methods (which reflect the directory entry type without following symlinks). `statSync` import removed. Pure internal-implementation change — function signature and behavior for real directories unchanged; all 21 existing tests pass.

- **Fix — Results screen title now names the bairro per ui-context.md Copy Conventions:**
  - **Finding:** The `fromNeighboringBairro` flag (unit 1.2/spec 004) was surfaced only via the `NearbyBanner` component ("Esta zona não tem abrigos registados. A mostrar abrigos de bairros vizinhos.") at `shelter-results.tsx:155`. The heading `title="Abrigos disponíveis"` (line 148) was identical regardless of whether results were local or from a neighboring bairro. This was a gap: neighboring-bairro results appeared under a generic title that misrepresented their proximity.
  - **Fix:** Changed the `title` prop to be dynamic:
    - Local results: `"Abrigos disponíveis em {bairroName}"`
    - Any result with `fromNeighboringBairro: true`: `"Abrigos próximos a {bairroName}"`
  - The eyebrow `contextLabel` (bairro name in 11px) remains unchanged per the spec's instruction.
   - **Tests:** Unit test in `shelter-results.test.tsx` asserts the local title variant renders in the live-fetch test and the cache test; a separate assertion in the nearby-banner test asserts the neighboring variant ("Abrigos próximos a Khongolote"). E2E tests in `shelter-search.spec.ts` updated from generic `getByText("Abrigos disponíveis")` to the specific bairro-qualified titles.

- **008 CI Pipeline** (`feature-specs/008-ci-pipeline.md`) — GitHub Actions workflow with 6 jobs at `.github/workflows/ci.yml`. Key details:
  - **Job order and dependencies:** Lint, TypeScript (typecheck), Unit Tests, and Integration Tests all run in parallel (no `needs`). Build has `needs: integration` — it seeds the same test database (`prisma migrate deploy` + seed), so it is sequenced after integration to avoid racing its truncate/seed cycle (concurrent truncation caused DB deadlocks). E2E Tests has `needs: build` so it only starts after build succeeds — not just sequential by ordering.
  - **Prisma client generation fix:** The `@prisma/client` postinstall (`prisma generate`) fails in CI when `DIRECT_URL` is unset because `prisma.config.ts` requires it. Fixed by adding an explicit `npx prisma generate` step with `DIRECT_URL` set to `TEST_DIRECT_URL` after `npm ci` in integration, build, and e2e jobs. This pattern also fixes `next build --turbopack` which needs the generated client to resolve TypeScript imports.
  - **Secrets:** `TEST_DIRECT_URL` (and via it `DIRECT_URL`) must be added to GitHub Actions secrets as `TEST_DIRECT_URL`. The workflow references `${{ secrets.TEST_DIRECT_URL }}` for both `DIRECT_URL` (Prisma generation) and `TEST_DIRECT_URL` (test execution). Without this, integration tests fail with `TEST_DIRECT_URL is not set`.
  - **Deliberate-break confirmation (step 4):** The href assertion in `app-header.test.tsx` was changed from `/explorar` to `/wrong-path` and pushed to `dev`. CI run 3 showed the Unit Tests job fail with annotation: `components/app-header.test.tsx:26` — expected `href="/wrong-path"`, received `href="/explorar"`. This confirms the pipeline correctly catches and surfaces test failures. The change was reverted in the next commit, and run 4 confirmed Unit Tests passed again.
  - **E2E fix — Turbopack `_not-found` crash in Playwright webServer subprocess:** Runs 2 and 5 E2E both failed with exit code 1 and no test-level annotations. Investigation revealed that `next build --turbopack` (Next.js 15.5.20) crashes with `Cannot find module for page: /_not-found` when spawned as a Playwright webServer subprocess, but succeeds when run directly from the shell. The root cause is a Turbopack subprocess bug — module resolution fails specifically when stdout is piped (Playwright captures webServer output). Fix: added `"build:e2e": "next build && npx tsx scripts/generate-sw.ts"` script and changed the Playwright webServer command to `npm run build:e2e && npx next start -p 3000` (non-Turbopack webpack build). The Turbopack-based `npm run build` is retained for the Build job, which succeeds in CI.
    - **Carry note:** This is a suspected upstream Next.js/Turbopack bug specific to spawning a build as a Playwright `webServer` subprocess with piped stdout. Worth revisiting on the next Next.js version bump to see if it's still needed — not something to carry indefinitely without a reason attached. If a future version of Next.js fixes the `_not-found` resolution when spawned with piped stdout, switch back to Turbopack for the e2e build (and remove the `build:e2e` script).
  - **Fix — `TEST_DIRECT_URL` added to the e2e job env block:** With the webServer switched to `npx tsx scripts/run-e2e-webserver.ts` (commit `fea73aa`, unpushed), that script hard-requires `TEST_DIRECT_URL` and exits 1 without it, but the e2e job's `env:` only set `DATABASE_URL`/`DIRECT_URL`. Verified against the last verified green CI run (`c23360b`, run `29672269564`) that the e2e job genuinely ran and passed (41s `npm run test:e2e` step, zero failure annotations) — **but** at that commit the webServer was still `npm run build:e2e && npx next start -p 3000`, so the missing var wasn't exercised there; it would have failed on the next push with the current tree. Added `TEST_DIRECT_URL: ${{ secrets.TEST_DIRECT_URL }}` to the e2e job env (mirroring the integration job).
  - **Enhancement:** Playwright now uses `reporter: process.env.CI ? [["list"], ["github"]] : "list"` so test failures produce inline GitHub Actions annotations.
  - **Pre-existing e2e test failures fixed — `selectOption`-on-combobox regression:** The GeographicPicker was migrated from native `<select>` to shadcn Select (Base UI combobox) in a previous unit, but the 3 e2e tests that exercise it (geographic-picker, shelter-search ×2) were never updated to match. The tests used `page.getByLabel("...").selectOption()` which only works on native `<select>` elements and throws `Error: Element is not a <select> element` on a `<button role="combobox">`. Fixed by creating a shared Playwright helper `selectOption(page, label, optionName)` at `tests/e2e/helpers.ts` that clicks the labelled trigger, then clicks the matching `getByRole("option")` — click-trigger → click-option pattern, exactly matching the RTL-level `selectOption` helper in `components/geographic-picker.test.tsx`. All 8 e2e tests now pass. This is the exact scenario that prompted the new test-layers rule in `ai-workflow-rules.md` (rule 5, paragraph beginning "When an instruction says to update 'the tests' for a changed component...") — the component's DOM structure changed but only the RTL tests were updated; the e2e layer was silently broken until now.
  - **Data-lifecycle fix — build and e2e now seed the test database independently:** The `integration` job seeds `TEST_DIRECT_URL`'s database in `beforeAll` and truncates in `afterAll`, but `build` and `e2e` both query the same database (snapshot generation in build, live `/api/shelters` calls in e2e) without ever seeding it themselves, and nothing sequences them relative to integration's seed/truncate cycle. Fixed by adding explicit `prisma migrate deploy` + seed steps to both `build` and `e2e` jobs. The `build` job additionally regenerates `geo-snapshot.json` and `shelters-snapshot.json` from the test database, keeping them consistent with the data the e2e tests query via `/api/shelters`. Each job seeds independently — neither depends on `integration`'s timing or leftover state.
  - **SW CSS fix — `public/sw.js` regenerated with Turbopack build:** The `public/sw.js` was generated by the webpack-based `build:e2e` script (from the Playwright webServer workaround), which puts CSS in `.next/static/css/` — a directory never scanned by `walkDir`. The unit test's regex at `scripts/__tests__/generate-sw.test.ts:34` matches `/_next/static/chunks/...css` and finds nothing. Regenerated with `npm run build` (Turbopack), which puts CSS in `chunks/` where `walkDir` finds it. Root cause: `walkDir` only scans `chunks/`, `media/`, and the build-ID directory — adding `css/` would make the SW format-agnostic, but since Turbopack is the primary build path and the webpack-based `build:e2e` is only used as a Playwright webServer workaround (carry note above), regenerating with Turbopack is the simpler fix. If the webpack build path becomes primary, `walkDir` should be extended to scan `.next/static/css/` as well.
  - **Safety fix — Database URL guardrail added to integration tests:** Each integration test file (`seed.test.ts`, `geo-snapshot.test.ts`, `shelters-snapshot.test.ts`) previously read `TEST_DIRECT_URL` and ran `prisma migrate deploy` + `TRUNCATE ... CASCADE` against whatever it resolved to, with no check against a misconfigured URL pointing at the real dev/prod database. Added `tests/integration/setup.ts` with `requireTestDbUrl()` and `createTestClient()`, now used by all three integration test files.
  - **Marker convention:** The `TEST_DIRECT_URL` connection string must contain the test project's Supabase ref (`ffetxfabaoafaqrrtoix`). The dev/prod project uses a different ref (`lnuwsgxnusuyvyqlbxba`), so this check distinguishes them. The ref appears in the URL's username segment (`postgres.ffetxfabaoafaqrrtoix@...`), which a dev/prod URL would never match. If the test project is migrated, update `TEST_PROJECT_REF` in `tests/integration/setup.ts`, `.env`, `.env.example`, and the CI secret.
  - **Failure mode:** `requireTestDbUrl()` throws a loud Error at import time — no migration, truncation, or seed runs. The error message includes the expected ref and confirmation that it was not found.
  - **Duplication removed:** The per-file `createClient()` function and its `PrismaPg` / `PrismaClient` boilerplate are replaced by the shared `createTestClient()`. Future integration test files should import `requireTestDbUrl` and `createTestClient` from `./setup` rather than duplicating the raw env-var read, PrismaPg adapter construction, or — critically — the guardrail-free URL access pattern. All 48 integration tests pass.
  - **Correction — CodeRabbit: substring check replaced with exact-username parse:** The first version of `requireTestDbUrl` used `dbUrl.includes(TEST_PROJECT_REF)`, which a crafted URL could bypass by embedding the ref in the password, a query parameter, a path segment, or any other non-username portion of the connection string (e.g. `postgresql://postgres.wrongref:ffetxfabaoafaqrrtoix@host/db` passes the substring check but targets a database with a different ref). Fixed by parsing via `new URL()` and asserting `parsed.username === "postgres.<TEST_PROJECT_REF>"` — the actual identity field Supabase embeds in connection strings. Also added `URL` parse-failure handling (malformed input throws clearly). A dedicated regression file `tests/integration/setup.test.ts` (9 tests) proves the fix matters: validates the ref-in-password bypass is rejected, the ref-in-query-param bypass is rejected, the ref-in-path bypass is rejected, a completely different dev/prod ref is rejected, URL parse failure is handled, and the valid case continues to pass. All 57 integration tests (9 new + 48 existing) pass.

- **Triggers:** Push to any branch + pull request targeting `main`. Concurrency group set to cancel in-progress runs on the same ref.
  - **Status visibility:** Check runs appear automatically on PRs per GitHub's default behavior — no extra configuration needed. **Pending — not yet verified on a real PR:** the CI runs so far are all on the `dev` branch, which proves the `push` trigger fires but not that check results render on an actual pull request (no PR has been opened against this repo yet). Confirm on a real PR before closing this item.
  - **No secrets committed:** `TEST_DIRECT_URL` lives only in GitHub Actions secrets. The workflow references it via `${{ secrets.TEST_DIRECT_URL }}`.
   - **Completed:** `TEST_DIRECT_URL` was added to GitHub Actions secrets (as confirmed by Integration Tests passing in CI runs 8+). All 6 CI jobs pass.

- **CodeRabbit doc/typing fixes batch (6 items):**
  - `TEST_DIRECT_URL` action item moved from "Human Steps Remaining" to completed.
  - `/abrigos` Server Component note (unit 1.2 era) marked historical — it is now a Client Component shell per 1.3b.
  - Native `<select>` architecture decision notes (×2) marked historical — migrated to shadcn/Base UI Select during visual polish.
  - `007-service-worker-precaching.md` spec updated: snapshots described as runtime-only (StaleWhileRevalidate), not precache+runtime, matching the deliberate implementation and documented Architecture Decision.
  - `roadmap.md` 1.3 cold-start description updated to include shelters-snapshot.json alongside geo-snapshot.
   - `lib/shelters/rank-and-filter.test.ts` missing `AdjacencyPair` import added — `tsc` was already failing on this (`TS2304: Cannot find name 'AdjacencyPair'` at line 163). Previously masked by no `tsc` script in `package.json` (only `npm run lint` which doesn't check types). All 67 unit tests, lint, and tsc pass clean after the fix.

- **Real gap closed — CI had no dedicated type-checking step:** `npm run lint` is ESLint-only, and `npm test` doesn't do full project type-checking — the genuine `TS2304` error went undetected through multiple CI runs.
  - `package.json` now has `"typecheck": "tsc --noEmit"`.
  - `.github/workflows/ci.yml` has a new `typecheck` job (name: "TypeScript") that runs in parallel with lint and unit tests: `npm ci` → `npx prisma generate` (needed for Prisma types) → `npm run typecheck`.
  - **First attempt was insufficient:** Excluding `**/*.test.ts`/`**/*.test.tsx` from the tsc scan would have missed the original `AdjacencyPair` bug (in a test file). The deliberate-break verification only proved the job catches errors in non-test files, which wasn't the motivating case.
  - **Corrected approach:** Test files are NOT excluded. Two pre-existing test-file issues were fixed instead:
    1. `vi`-global not found: Only `app/page.test.tsx` uses `vi` as an unimported global (other test files import it from vitest explicitly). Added `/// <reference types="vitest/globals" />` to that file.
    2. Pre-existing `TS2786` (`'Home' type '() => void' not a valid JSX component`): The `redirect()` function returns `never`, but `tsc` inferred `Home`'s return type as `void` in the test-file context. Fixed by adding an explicit `: never` return type to the `Home` component in `app/page.tsx`. This error was previously masked by the `TS2304: 'vi' not found` errors that occurred first.
  - **Deliberate-break re-verification:** Reintroduced the original `AdjacencyPair`-style bug by removing `AdjacencyPair` from the import in `lib/shelters/rank-and-filter.test.ts` (a test file). Pushed — the TypeScript CI job failed with `TS2304: Cannot find name 'AdjacencyPair'` at line 163, proving test-file type errors are now caught. Reverted. The reverted commit's CI confirmed all jobs green, including TypeScript.

- **Fix — Deterministic seed ids (offline snapshot integrity):** All geographic models previously relied on `@default(uuid())` (UUIDv4), so every re-seed produced new random ids and the committed `public/geo-snapshot.json` / `public/shelters-snapshot.json` (whose ids key the offline Dexie cache) went stale after any re-seed — a silent offline-data-integrity issue, not cosmetic.
  - **Change:** `prisma/seed.ts` now passes explicit ids derived via UUIDv5 from a stable natural key (ancestry path + name). New pure helper at `scripts/lib/deterministic-id.ts` (`uuidv5(name, namespace?)` + `stableId(...parts)`), SHA-1-based RFC 4122 UUIDv5 over the standard DNS namespace, no new dependency. Key scheme: `provincia:<name>`, `distrito:<prov><distrito>`, `bairro:<prov><dist><bairro>`, `quarteirao:<prov><dist><bairro><quarteirao>`, `shelter:<prov><dist><bairro><name>` — the shelter key deliberately excludes its quarteirao (a shelter's stable identity is bairro+name, so it keeps its id if reassigned to a different quarteirão between seeds). Schema defaults unchanged (`@default(uuid())` remains as the DB-level fallback for future runtime writes); no migration required.
  - **Proof (not just the helper existing):** Ran the seed against the test database twice in succession and regenerated both snapshots after each run — the two runs produced byte-identical `geo-snapshot.json` and `shelters-snapshot.json`. All 136 ids across both snapshots are UUIDv5 (version digit `5`, zero UUIDv4 left). New integration test `seed.test.ts` "re-seeding produces identical ids for every geographic row and shelter" records all ids across all 6 tables, re-runs `runSeed`, and asserts deep equality.
  - **Tests updated (flagged first):** `tests/e2e/cold-start-offline.spec.ts` hardcoded the old random UUIDs (EPC Khongolote `92044824-…`, Igreja Católica `97f36b6b-…`, Salão Paroquial `6d14f498-…`, quarteiraoId `b26b1100-…`) in its `/api/shelters` mock. Those were mock-only (never cross-referenced against snapshot/DB data), so the test wouldn't break — but they were updated to the new deterministic values to stay aligned with the shipped snapshots. No integration/unit test asserted a specific UUID literal or relied on ids being freshly random; all resolve ids dynamically by name (`findFirst`) — confirmed by searching for UUID literals across tests.
  - **Snapshot regeneration:** Dev DB (`postgres.lnuwsgxnusuyvyqlbxba`) is currently unreachable, so the snapshots were regenerated from the test database — which is equally valid because ids are pure functions of the natural keys, independent of which database ran the seed.
  - **Verification:** 6 new unit tests (`scripts/lib/deterministic-id.test.ts`), 58 integration tests (1 new determinism test) pass; all 9 e2e tests pass (cold-start offline suite exercises the regenerated snapshots through the offline Dexie path); lint and `tsc --noEmit` clean. Note: local e2e runs require the test DB to be seeded first (`DIRECT_URL="$TEST_DIRECT_URL" npx tsx prisma/seed.ts`) — the integration suite's `afterAll` truncates, and the shelter-search e2e tests hit `/api/shelters` live against that DB (the cold-start offline tests mock `/api/shelters`, which is why they pass with an empty DB). This matches the CI fix already documented for the build/e2e jobs seeding independently.
  - **Follow-up 1 — dev DB reachability was transient, not a real outage:** The initial `(ENOTFOUND) tenant/user postgres.lnuwsgxnusuyvyqlbxba not found` from `prisma db seed` was a transient Supabase pooler routing failure. Subsequent checks against the dev connection (`aws-0-eu-west-1.pooler.supabase.com:5432`, project ref `lnuwsgxnusuyvyqlbxba`): DNS resolves, TCP:5432 connects, the direct host `db.lnuwsgxnusuyvyqlbxba.supabase.co` resolves (a paused/deleted project would not), and a `pg` client connection to `DIRECT_URL` succeeds. The dev project is up and was not paused or misconfigured.
   - **Follow-up 2 — dev runs the identical seed data; committed snapshots match what dev produces:** Before touching dev, a read-only comparison showed dev's geo name tree (all 4 provinces → 5 distritos → 22 bairros → 30 quarteiroes) is identical to the committed snapshot content (order-insensitive), all 25 dev shelter records (name + tier + capacityStatus + routeDescription) match the committed shelters snapshot exactly, and dev counts (4/5/22/30/25/26/8 users) match seed output — no manual entries, no divergent seed history (dev still held the old random-id seed from the same `prisma/seed.ts`). Definitive proof: reseeded dev with the new deterministic seed (`npx prisma db seed`) and regenerated both snapshots **from dev** (`npm run generate:*` against `DIRECT_URL`) — both are byte-identical to the committed files. Dev now holds 59/59 UUIDv5 ids (0 UUIDv4) with EPC Khongolote = `7b87d0a5-5663-510e-b0e3-4177d36d2240`, matching the committed snapshots and the e2e mock. Rationale for generating from the test DB in the first place was therefore sound, and is now superseded: the snapshots have been regenerated from the actual dev database, so the committed files are the dev DB's own output.

- **Fix — Select token-naming drift (`focus:bg-accent/10` and `bg-border` → canonical `accent-brand`/`border-default`):** `components/ui/select.tsx:176` used `focus:bg-accent/10` for the focused-option highlight and `:202` used `bg-border` for `SelectSeparator`, while the rest of the file and the design system use `accent-brand`/`border-default`. **Important correction to the reported premise:** this was **not** silently-missing CSS. Both `--color-accent` (`globals.css:44`) and `--color-border` (`globals.css:47`) ARE defined in `@theme inline` (they are the shadcn-compat aliases, alongside the canonical `--color-accent-brand` at :15 and `--color-border-default` at :12), so both classes compiled to real rules (`.focus\:bg-accent\/10:focus{background-color:color-mix(in oklab,var(--accent)10%,transparent)}` and `.bg-border{background-color:var(--border)}` verified in the built CSS). The real issue is convention drift: the two lines referenced the compat aliases instead of the project's canonical tokens. Both changed to `focus:bg-accent-brand/10` and `bg-border-default` — behavior-preserving (identical resolved values), verified in a fresh build that `.focus\:bg-accent-brand\/10:focus{...color-mix(...var(--accent)10%...)}` and `.bg-border-default{background-color:var(--border)}` are now emitted.
  - **Rendered verification (real browser, Playwright/Chromium against `/explorar` on a dev server):** Opened the Província dropdown and pressed ArrowDown. Focused option "Gaza" reported `document.activeElement === el`, `data-highlighted` set, and computed `background-color: oklab(0.56952 0.055143 0.106132 / 0.1)` (= light-theme `--accent` #a8651b at 10%) while the other three options computed `rgba(0,0,0,0)` — the highlight genuinely paints on the focused row only. Item separator: every option computed `border-bottom: 1px solid rgb(228,218,197)` (= light-theme `--border` #e4dac5). `SelectSeparator` (`bg-border-default`) was additionally mounted temporarily in the dropdown to verify it actually renders: computed `background-color: rgb(228,218,197)`, height 1px; then reverted. Screenshots saved at `/tmp/opencode/dropdown-open.png`, `/tmp/opencode/dropdown-focused.png`, `/tmp/opencode/separator-rendered.png`.
  - **Side-finding:** Tailwind v4's content scan includes `context/*.md`, so the prose mention of `focus:bg-accent/10` in this file keeps generating a (dead, harmless) `.focus\:bg-accent\/10` rule in the compiled CSS even after the code was fixed — not a bug, but worth knowing when grepping for what's actually in the bundle.
  - All 73 unit tests pass (one `geographic-picker` test timed out once under parallel load and passed in isolation + on re-run — pre-existing flakiness, unrelated to this class-name change), lint and `tsc --noEmit` clean. `public/sw.js`/`sw.js.map` regenerated by the verification build were reverted to keep this diff focused on `components/ui/select.tsx`.

- **Documentation-alignment batch (8 small fixes, no logic changes):**
  - **(1) PR check-visibility note marked pending:** The 008 tracker entry's status-visibility note previously claimed checks were "Verified by the multiple CI runs visible on the `dev` branch" — branch runs only prove the `push` trigger fires, not that checks render on an actual pull request. No PR has been opened against this repo yet, so it's now marked **Pending — confirm on a real PR** before closing.
  - **(2) CI job count corrected from five to six:** The tracker summary (Current Goal, the 008 entry heading, and the Completed note) referenced "5 jobs"/"5 staged jobs" — the `typecheck` job was added later and the summaries weren't updated. Now six: lint, unit, integration, build, e2e, typecheck.
  - **(3) Unit 1.1's native-`<select>` entry marked historical:** The "four styled native `<select>` elements … component tests (9 tests)" note described the pre-migration state; it's now flagged as historical with a pointer to the shadcn/Base UI `Select` migration (12 component tests).
  - **(4) Architecture Decision "spec lists snapshots in the precache list" clarified:** That sentence described spec 007's *original* draft wording; the spec has since been corrected to runtime-only (StaleWhileRevalidate) caching. The decision now says so explicitly.
  - **(5) Spec 002's test-isolation step rewritten:** Implementation step 5 still described the original disposable-schema approach (`?schema=`), which didn't work with the `PrismaPg` adapter. It now describes the actual implementation: a dedicated `TEST_DIRECT_URL` Supabase project plus the `requireTestDbUrl()` URL-parsing guardrail in `tests/integration/setup.ts` (username-segment ref check before any migration/truncation/seed).
  - **(6) `code-standards.md` Server/Client exception rule updated:** The rule claimed a page needing offline fallback "must be a Client Component, in full" — the actual 1.3b implementation has a thin Server Component page wrapper (`abrigos/page.tsx`) with `ShelterResults` (Client Component child) owning the fetch/Dexie fallback. The rule now describes the thin-Server-wrapper form as the acceptable shape.
  - **(7) Spec 004's server-rendered design marked as the unit 1.2 baseline:** The "Data flow" paragraph and the "results page is a Server Component" checklist item now carry a historical marker noting the 1.3b replacement (thin Server wrapper + client-side `ShelterResults` fallback shell).
   - **(8) Spec 008's design text corrected to the actual dependency graph:** The original text claimed lint/unit "fail fast" before integration/build/e2e and listed a staged 5-job order. The actual workflow runs lint, typecheck, unit, and integration independently in parallel; **build has `needs: integration`** (shared test-DB sequencing — it seeds the same database and must not race integration's truncate/seed cycle); e2e has `needs: build`. Note: the initial batch description assumed build also ran independently, but `ci.yml`'s `build` job declares `needs: integration`, so the doc reflects that reality.

- **Fix batch — fetch calls without timeout hang forever on a stalled connection (one bug shape, three files):** `geographic-picker.tsx`, `shelter-results.tsx`, and all three fetches in `syncData()` called `fetch` with no abort-based timeout. On a stalled-but-not-dead connection, `fetch` neither resolves nor rejects, so the offline/cache fallback (which only runs in the `catch`) never triggers — the UI stays on "A carregar..." indefinitely even though cached data is available. This undermined the 1.3b/1.3c offline-reliability work.
  - **Shared helper instead of triplicated logic:** New `lib/offline/fetch-with-timeout.ts` exporting `fetchWithTimeout(input, init?, timeoutMs?)` and `DEFAULT_FETCH_TIMEOUT_MS = 8000`. It wraps `fetch` with its own `AbortController` + `setTimeout(() => controller.abort(), timeoutMs)`, forwards an existing `init.signal` (aborting the controller if it's already aborted, or wiring it to abort the controller), passes `controller.signal` through to `fetch`, and `clearTimeout`s the timer in `.finally` once the request settles. The 8s default is consistent across all three call sites (single fetch vs. the `Promise.all` — each of the three `sync.ts` fetches gets its own timeout, and `Promise.all` rejects on the first one; the later rejections are handled by `Promise.all`'s per-input handlers, so no unhandled rejections).
  - **Chose AbortController + setTimeout over `AbortSignal.timeout`:** Verified empirically that `AbortSignal.timeout`'s internal timer is **not** controlled by vitest's fake timers (`vi.advanceTimersByTimeAsync` does not fire it), which would make the hang-then-fallback tests take 8 real seconds each (or require abstract mock plumbing). The `setTimeout`-based controller is fully fake-timer-controllable, so each hang test proves the real behavior deterministically in milliseconds. `AbortSignal.any`/`AbortSignal.timeout` are both present in jsdom 29 but unused for the same testability reason.
  - **Tests prove hang-then-fallback, not just "a timeout parameter exists":** 7 new tests, all simulating a never-settling fetch (a promise that only rejects when its abort signal fires) and advancing fake timers past the timeout:
    - `lib/offline/fetch-with-timeout.test.ts` (4): resolves fast + clears its timer, passes init through alongside the abort signal, rejects after an explicit 1s timeout instead of hanging, and aborts after the default 8s when no timeout is given.
    - `components/geographic-picker.test.tsx` (1): snapshot fetch hangs → after the timeout the component falls back to the Dexie-cached snapshot and renders the "Sem ligação" staleness banner instead of staying on "A carregar...".
    - `components/shelter-results.test.tsx` (1): `/api/shelters` hangs → after the timeout the component renders cached shelters with the staleness banner.
    - `lib/offline/sync.test.ts` (1): the geo-snapshot fetch hangs → `syncData()` rejects after the timeout instead of hanging forever (leaving the existing Dexie cache intact, matching `SyncOnLoad`'s `.catch(() => {})`).
  - **Test-writing detail worth recording:** the rejection handler must be attached *before* `vi.advanceTimersByTimeAsync` runs (e.g. `const rejection = expect(promise).rejects.toThrow()` first), because that helper flushes the microtask queue internally — a promise that rejects mid-advance with no handler yet attached surfaces as an unhandled-rejection error even though the test eventually awaits it.
  - `vi.useRealTimers()` added at the top of each affected test's `beforeEach` so the fake-timer hang tests can't leak into the rest of the suite.
  - All 80 unit tests pass (7 new), `npm run typecheck` and `npm run lint` clean.

- **Fix batch — Prisma client generation & build-lifecycle (3 related issues):**

  - **Issue 1 — No implicit `prisma generate` on install (Prisma 7):** `@prisma/client@7.8.0` has no `postinstall` script (confirmed from its published `package.json` and from `package-lock.json`'s `hasInstallScript` list — only `prisma`, `@prisma/engines`, `esbuild`, `sharp`, `fsevents`, `unrs-resolver` have install scripts). A clean `npm ci` does **not** generate the client — proven by `rm -rf lib/generated/prisma && npm ci` (the `lib/generated/` directory remains empty). Without explicit generation, `npm run build` fails with `Module not found: Can't resolve '@/lib/generated/prisma/client'`, and `tsc --noEmit` fails with `TS2307: Cannot find module '@/lib/generated/prisma/client'`. **Fix:** Added explicit `prisma generate &&` to both `build` and `typecheck` scripts in `package.json`, and to `build:e2e` for consistency. The CI pipeline was already running explicit `npx prisma generate` in the typecheck, integration, build, and e2e jobs — confirmed this is complete coverage: the lint and unit jobs do not need it (verified by running both against a checkout with the generated client removed — both passed). The `.gitignore` line `/lib/generated/prisma` is correct and unchanged. Clean-checkout simulation (`rm -rf node_modules lib/generated/prisma && npm ci && npx prisma generate && npm run build`) confirmed the target sequence works end to end.

  - **Issue 2 — `PrismaPg` adapter constructed at module evaluation time, leaking a Pool on every Next.js hot reload:** Previously:
    ```ts
    const adapter = new PrismaPg({ connectionString: process.env["DATABASE_URL"] });
    export const prisma = globalForPrisma.prisma ?? new PrismaClient({ adapter });
    ```
    The `new PrismaPg(...)` runs unconditionally every time the module is imported, including during Next.js dev hot reloads where the cached `globalForPrisma.prisma` wins — the newly constructed `PrismaPg` Pool is immediately orphaned, never closed, and leaks connections against the Supabase pooler. **Fix:** Moved adapter construction into a `createPrismaClient()` function that is only called when no cached global client exists:
    ```ts
    function createPrismaClient(): PrismaClient {
      if (!process.env["DATABASE_URL"]) throw new Error("DATABASE_URL is not set. …");
      return new PrismaClient({ adapter: new PrismaPg({ connectionString: process.env["DATABASE_URL"] }) });
    }
    export const prisma = globalForPrisma.prisma ?? createPrismaClient();
    ```
    **DATABASE_URL check:** Added an explicit throw with a clear error message when `DATABASE_URL` is missing, replacing the previous silent construction of a `PrismaPg` with `undefined` connectionString (`PrismaPg({ connectionString: undefined })` was confirmed not to throw immediately — it orphaned a broken Pool that failed confusingly on first query). **CI integration check:** The integration test job runs without `.env` and previously had no `DATABASE_URL` env var — with the new throw, module import in integration tests (via `search.ts` → `prisma-client.ts`) would have crashed. Added `DATABASE_URL: ${{ secrets.TEST_DIRECT_URL }}` to the integration job's `env:` block (mirroring the e2e job which already set it). **Test:** New permanent test at `lib/db/prisma-client.test.ts` (3 tests) mocks `@prisma/adapter-pg` with a constructor counter and verifies: (a) hot reload re-evaluation reuses the cached client and constructs zero additional adapters (count stays 1), (b) a fresh context (no cached client) constructs exactly one new adapter (count goes to 2), (c) missing `DATABASE_URL` throws the expected error. All 83 unit tests pass (3 new), typecheck and lint clean.

   - **Issue 3 — `@prisma/client` was in `devDependencies` but is a runtime dependency:** A production-only install (`npm ci --omit=dev`, Vercel's default) would omit `@prisma/client` and break the server-side database layer at runtime. The generated client imports from `@prisma/client/runtime/client` — this is genuinely needed at runtime. **Fix:** Moved `@prisma/client` from `devDependencies` to `dependencies` (alongside `@prisma/adapter-pg` and `pg`, the two other genuine runtime database dependencies). `prisma` (the CLI) correctly stays in `devDependencies`. `package-lock.json` regenerated via `npm install` to reflect the change. Clean-checkout simulation (`npm ci && npm run build`) confirmed no breakage.

- **Fix — Windows entrypoint guard bug:** The CLI-entry guard at `prisma/seed.ts:614` checked `process.argv[1]?.endsWith("prisma/seed.ts")`, but on Windows `process.argv[1]` uses backslash separators (`@prisma\seed.ts`), so the check silently returned false. `prisma db seed` would exit with code 0 while seeding nothing at all, with no error to indicate anything went wrong. Fixed by normalizing path separators before comparison: `process.argv[1]?.replace(/\\/g, "/").endsWith("prisma/seed.ts")`.

- **Fix — Cross-distrito neighbours silently dropped:** The `bairroIdByName` map was rebuilt fresh inside each distrito's loop (`prisma/seed.ts:486`), so a `vizinhos` entry naming a bairro in a different distrito resolved to `undefined` and was silently skipped with no error or log. This didn't affect the current seed data (all neighbour pairs are intra-distrito), but any future edit adding a cross-distrito adjacency would silently vanish with zero signal. Fixed by throwing a clear error when a named neighbour doesn't resolve, following the "fail loud on data that doesn't resolve as expected" pattern already used elsewhere in the project's seed/guard logic.
